# Created by https://www.toptal.com/developers/gitignore/api/ansible,pulumi,terraform
# Edit at https://www.toptal.com/developers/gitignore?templates=ansible,pulumi,terraform

**/.env

### Ansible ###
**/*.retry
**/group_vars/all.yml

### Pulumi ###
# Ignore temp build directory for Pulumi
# Info: https://www.pulumi.com/docs/

**/.pulumi/
**/*.pyc
**/venv/
**/__pycache__
**/Pulumi.dev.yaml

### Terraform ###
# Local .terraform directories
**/.terraform

# .tfstate files
**/*.tfstate
**/*.tfstate.*

# Crash log files
**/crash.log
**/crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
**/*.tfvars
**/*.tfvars.json

# Include example file
!**/variables.auto.example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
**/override.tf
**/override.tf.json
**/*_override.tf
**/*_override.tf.json

# Ignore CLI configuration files
**/.terraformrc
**/terraform.rc

# End of https://www.toptal.com/developers/gitignore/api/ansible,pulumi,terraform
